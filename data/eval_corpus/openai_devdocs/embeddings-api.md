# Embeddings API

## Request Shape

- The embeddings endpoint takes a model.
- The embeddings endpoint takes input text or a text array.

## Returned Data

- The endpoint returns embedding vectors.
- The endpoint returns usage metadata.
- Embedding vectors are numeric arrays for retrieval or clustering.

## Boundary

- The embeddings endpoint creates vectors.
- The embeddings endpoint does not perform retrieval by itself.
